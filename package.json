{
  "name": "shivam-traders",
  "private": true,
  "description": "Root wrapper for Render — API lives in server/",
  "scripts": {
    "start": "npm start --prefix server",
    "postinstall": "npm install --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
